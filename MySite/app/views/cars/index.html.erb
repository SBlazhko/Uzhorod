<div class="panel panel-default">
<div class="panel-heading">Car list</div>
<p id="notice"><%= notice %></p>

<table class="table">
  <thead>
    <tr>
      <th>Сars name </th>
      <th>Photo</th>
      <th> Original Price</th>
      <th> UAH </th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @cars.each do |car| %>
      <tr>
        <td><%= car.title %></td>
        <td><%= image_tag car.car_images.first.avatar.url(:thumb) %></td>
        <td><%= car.price.format %></td>
        <td><%= car.uah_price.format %></td>
        <td><%= link_to 'Details', car %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

</div>
</div>
<%= link_to 'New Car', new_car_path %>
